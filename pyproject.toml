[tool.pylint.messages_control]
disable = "bad-continuation, invalid-name, missing-class-docstring, missing-function-docstring, missing-module-docstring, unused-argument"

[tool.pylint.format]
max-line-length = "88"

[tool.pylint.similarities]
ignore-imports = "yes"

[tool.isort]
profile = "black"
